purchases_df['Age Groups'] = pd.cut(purchases_df['Age'],bins = bins,labels= age_labels)
total_count = total_by_age = players_by_gender_df['SN'].nunique()
 
each_age = gender_purchases['Age'].value_counts()

#percentage = (each_age / player_names) * 100 


#age_demographic = pd.DataFrame({"Total Count":  each_age,
                                #"Percentage of Players":percentage}                                )


age_demographic



age_demographic = pd.DataFrame({"Total Count":  total_by_age,
                                "Percentage of Players":new}                                )


age_demographic

